/** Standard ESP32 flash sizes (MB), ascending. Drives the Flash dropdown and
 *  flash-size inference. Keep ascending so the smallest fitting size wins. */
export const FLASH_OPTIONS_MB = [2, 4, 8, 16, 32, 64, 128, 256, 512];
